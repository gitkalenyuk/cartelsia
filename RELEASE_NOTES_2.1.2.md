# Cartelsia 2.1.2 — виправлення автореєстрації + проксі 2.0

**Дата:** 03.09.2026

## Головне: реєстрація знову працює

**Причина зламання (живо підтверджено):** Cartesia **забанила слово «cartelia»**.
Старий генератор робив `cartelia_mt8web7_giyb@saputti365.life` + імʼя «cartelia» →
червона помилка «This action couldn't be completed» на формі (5 з 5 контрольних
спроб заблоковані).

**Доведено живими реєстраціями (03.09.2026, headless Chromium):**
- ✅ `maple91kite@saputti365.life` (word-стиль) → **повна реєстрація за 29 с**
- ✅ `support4772026090310@saputti365.life` (support-стиль) → **повна реєстрація за 35 с**
- ❌ `cartelia_mtlafrkx_5u6t@…` (старий стиль) → заблоковано на формі, 5/5

## 1. Нова система генерації email (4 стилі у Налаштуваннях)

| Стиль | Вигляд | Коли юзати |
|---|---|---|
| **Random** (default) | `mt8kb1dc8llr@saputti365.life` | Найбезпечніший — випадкові 10–12 символів |
| **Слова** | `stone42river@saputti365.life` | Природніший вигляд |
| **Support** | `support0010309202061014@saputti365.life` | Вигляд техпідтримки |
| **Свій префікс** | `john7xk2m9a@…` | Ваш префікс + рандом |

- Слово «cartelia» (і все інші бренд-слова) більше **НІКОЛИ** не зʼявляється —
  ані в email, ані в іменах, ані в паролях.
- Імена тепер **людські** (John Smith, Maria Wright…) замість кривих наборів
  із лок-частини адреси.
- Регрес-тест у юніт-тестах: жоден стиль не може видати бренд-слово.

## 2. Зупинка і вихід — все реально вмирає

- **Стоп** автореєстрації = **негайне вбивство всіх браузерів** (killAll у
  registrar; registerOne кидає помилку і потоки завершуються).
- **Закриття вікна = повний вихід**: вбиваються браузери, інтераптить авторег,
  flush усіх файлів. Застосунок більше **не залишається у фоновому режимі**
  після закриття.
- **На старті** застосунок прибирає осиротілі headless Chromium попереднього
  запуску (wmic-скан за ms-playwright command line) — більше не треба
  вбивати процеси руками через диспетчер.

## 3. Проксі 2.0

- **Імпорт із файлу** (нативний діалог, .txt/.csv).
- **Realtime чекінг**: статус кожного проксі зʼявляється **в міру перевірки**
  («перевіряється…» → живий/мертвий), а не після перевірки всіх.
- **Потоки чекінгу** (1–50, default 10) і **таймаут** (default 12 с) —
  налаштовуються прямо у вкладці Проксі.
- **Зупинка чекінгу** працює: недоперевірені повертаються у «не перевірений».
- **Експорт** списку в файл.
- **Persist**: список проксі зберігається у `data/proxies.json` — переживає рестарт.
- **socks5://** підтримка в імпорті.

## 4. Дрібні виправлення

- **Композер**: textarea **авто-розширюється** під текст (до 45% висоти вікна,
  далі скрол) — більше не «стрибає» при вставці великого тексту.
- **Іконка**: новий логотип YT Cartel (скелет з надписом, без фону) — у заголовку
  вікна, панелі задач і в сайдбарі застосунку.

## Тести

**153/153** unit-тестів зелені (+14 нових: identity-стилі, санітизація,
валідація, парсинг проксі зі socks5). Typecheck чистий.
Живі e2e реєстрації: 2 успішні (word + support стилі) до фіксації rate-limit.
