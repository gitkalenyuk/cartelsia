# Cartelsia 2.1.1 — sonic-3.6 + locale

**Дата:** 27.08.2026

## Суть релізу

Cartesia випустила нову модель **sonic-3.6** (GA, 27.08.2026) — Cartelsia оновлена:
нова модель за замовчуванням, вибір скрізь (композер + налаштування), плюс підтримка
нового поля **locale** (регіональна вимова дат/чисел).

## Перевірено наживо (живі виклики api.cartesia.ai, 27.08.2026)

| Тест | Результат |
|---|---|
| `sonic-3.6` /tts/bytes uk | ✅ 200, WAV 2.64 с |
| `sonic-3.6-2026-08-27` (датаваний знімок) | ✅ 200 |
| `sonic-3.5` і `sonic-3` (старі) | ✅ досі працюють — нічого не зламано |
| **API не змінено**: старий `Cartesia-Version: 2026-03-01` приймає 3.6 | ✅ |
| SSE (субтитри) з 3.6: chunks + word_timestamps | ✅ 200 |
| `locale` + `language` РАЗОМ | ⚠️ 400 «mutually exclusive» — враховано в коді |
| `locale`-only `en-GB`, `uk-UA` | ✅ 200 |
| `generation_config.emotion` з 3.6 | ✅ 200 |
| Невалідна модель | 404 `model_not_found` + request_id (класифікація вже була) |

## Що зроблено

- **ModelId**: +`sonic-3.6`; дефолт генерацій тепер sonic-3.6 (старі чати з 3.5/3.3 сумісні — union розширено).
- **Вибір моделі скрізь**: композер (3 варіанти з описами) + Налаштування → Генерація.
- **Locale-селектор** у композері (видимий тільки для sonic-3.6): en-GB, uk-UA, de-DE, pl-PL, fr-CA…
  Вибір locale автоматично прибирає language із запиту (взаємовиключність API), і навпаки.
- **ttsBody**: locale > language, ніколи не шле обидва.
- Старі голоси/клони/спільні голоси працюють з 3.6 без змін (PVC, створені на 3.5, сумісні — офіційно).

## Тести

- **139/139** unit-тестів зелені (+5 нових: контракт locale/language взаємовиключності,
  дефолт моделі, back-compat union).
- Живий e2e через повний клієнт: bytes 3.44 c WAV + SSE 4 слова з таймкодами + помилка з request_id.
- Typecheck main+renderer чистий.
